# Base URL of the AgileEstimation.API project (no trailing slash)
VITE_API_BASE_URL=https://localhost:7248

# SignalR hub path is appended to the API base URL automatically,
# but you can override the full hub URL here if it's hosted elsewhere.
VITE_SIGNALR_HUB_URL=https://localhost:7248/hubs/planning-poker
